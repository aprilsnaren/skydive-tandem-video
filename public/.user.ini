; PHP ini overrides for chunked video upload.
; Each request contains only one 5MB chunk — no large memory needed.
memory_limit = 128M
upload_max_filesize = 10M
post_max_size = 10M
max_execution_time = 30
